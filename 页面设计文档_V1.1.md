# 页面设计文档（UI/UX Spec）

## 旅途管家 —— 个人旅游顾问与线路推荐小程序

| 项 | 内容 |
| --- | --- |
| 版本 | V1.2（配套需求说明书 V1.2） |
| 范围 | 用户端小程序 + Web 管理后台 |
| 最近更新 | 2026-07-24 |
| 对应文档 | `需求说明书_修订版V1.1.md` |

---

## 1. 文档概述

### 1.1 目的
将需求说明书中的功能映射为可落地的页面、布局、组件与交互，作为 UI 设计与前端开发的直接依据。

### 1.2 设计原则
- **信任感优先**：旅游是高决策成本消费，页面须突出顾问真人形象、真实点评、透明费用。
- **少输入多选择**：报名/需求提交以「选择 + 少量填空」为主，降低操作门槛。
- **私域温度**：强调「专属顾问」「1v1」，弱化电商货架感。
- **一致性**：全端复用同一套设计系统（色彩/组件/间距）。

---

## 2. 设计系统（Design System）

### 2.1 色彩

| 名称 | 色值 | 用途 |
| --- | --- | --- |
| 主色 Primary | `#2B7FFF`（蓝） | 主按钮、关键链接、选中态 |
| 辅助色 Accent | `#FF8A3D`（橙） | 优惠标签、CTA、价格强调 |
| 成功 | `#2BBF6A` | 成团/报名成功 |
| 警示 | `#FF4D4F` | 库存紧张、退改提示 |
| 文字主 | `#1A1A1A` | 标题/正文 |
| 文字次 | `#6B7280` | 辅助说明 |
| 背景 | `#F5F7FA` | 页面底色 |
| 卡片 | `#FFFFFF` | 卡片底 |

### 2.2 字体与间距
- 字号：标题 34rpx / 正文 28rpx / 辅助 24rpx。
- 圆角：卡片 16rpx，按钮 48rpx（胶囊），标签 8rpx。
- 间距基准：8rpx 栅格（8/16/24/32）。
- 字体：小程序默认 `-apple-system` / `PingFang SC`。

### 2.3 基础组件库
按钮（主/次/文字）、卡片、标签、价格、评分星、步骤条、弹窗、Toast、空状态、列表项、底部固定操作栏。

---

## 3. 信息架构

### 3.1 小程序端导航（TabBar）
```
[首页]   [线路]   [智能规划]   [我的]
```
- 首页：品牌 + 热门推荐 + 快捷入口
- 线路：分类 + 列表 + 搜索
- 智能规划：需求提交（MVP）/ AI 规划（二阶段）
- 我的：订单、收藏、客服、关于

### 3.2 页面树（用户端）
```
首页
├ 线路分类页
├ 线路列表页
├ 线路详情页
│   └ 报名表单页
├ 智能规划页（需求提交）
│   └ 需求提交成功页
├ 我的
│   ├ 订单列表页
│   ├ 订单详情页
│   ├ 我的收藏页
│   ├ 关于顾问页
│   └ 客服/咨询页
├ 客户注册页（手机号注册/登录）
└ 分享卡片（小程序分享链路）
```

### 3.3 管理后台（Web）
```
登录 → Dashboard
├ 线路管理（列表/新增编辑）
├ 订单管理（列表/详情/确认定金）
├ 客户 CRM（列表/详情/标签）
├ 收益管理（统计/明细）
├ 轮播管理（Banner 增删改查）
├ 营销管理（二阶段）
└ 系统设置 / 用户权限
```

---

## 4. 用户端页面详细设计

### 4.1 首页（Home）
**目标**：建立信任、引导浏览与规划。

```
┌─────────────────────────┐
│ [顾问头像] 嗨，我是顾问小旅 │  ← 真人形象卡
│ 专注云南/新疆深度游 8 年   │
├─────────────────────────┤
│ [Banner 轮播 x3]          │
├─────────────────────────┤
│ 热门推荐       更多 >      │
│ [卡][卡][卡] 横滑          │  ← 线路卡片
├─────────────────────────┤
│ 快捷入口                    │
│ [智能规划][热门路线]        │
│ [私人定制][我的订单]        │
├─────────────────────────┤
│ 顾问说（最新动态/攻略）     │
└─────────────────────────┘
```
- 顾问卡点击 → 进入「关于顾问 / 客服」。
- Banner 点击 → 跳转对应线路详情。
- 线路卡片：封面图 + 名称 + 天数 + 价格 + 热度标。

### 4.2 线路分类 / 列表页（Route List）
**目标**：按分类/关键词找线路。

```
顶部：搜索框 + 分类 Tab（国内/短途/境外）
筛选：天数 / 出发地 / 价格
列表：线路卡片（竖向）
```
- 分类 Tab 对应需求 7.2 分类树。
- 列表项复用首页线路卡片组件。

### 4.3 线路详情页（Route Detail）
**目标**：促成报名，透明展示费用。

```
┌─────────────────────────┐
│ [图片/视频 轮播]          │
│ ★★★★★ 4.9  已报名28/成团20│
├─────────────────────────┤
│ 新疆15日深度游             │
│ 上海出发 · 15天 · ¥3999    │  ← 价格强调橙
├─────────────────────────┤
│ 行程安排  Day1…Day15 可展开 │
│ 费用说明（含/不含）         │
│ 注意事项                   │
├─────────────────────────┤
│ [收藏]   [立即报名]        │  ← 底部固定栏
└─────────────────────────┘
```
- 成本/利润字段**不展示**（仅后台）。
- 「已报名/成团」用步骤条或进度提示，临近成团用警示色。

### 4.4 报名表单页（Signup）
**目标**：低门槛提交报名。

```
线路摘要（名称/日期/价格）
姓名 *       [____]
手机号 *     [____]
人数 *       [- 2 +]
出发日期 *   [日期选择]
备注         [____]
[提交报名]
```
- 提交后进入「待确认」，订单状态见 7.5。
- MVP 不支付，提示「顾问将联系您确认定金（线下）」。

### 4.5 智能规划页（MVP：需求提交）
**目标**：承接核心入口，MVP 以需求单替代 AI。

```
标题：智能行程规划（顾问 1v1 帮您规划）
目的地   [不限 ▾]
时间     [5天 ▾ / 自定义]
人数     [夫妻2人 ▾]
预算     [¥5000 ▾]
兴趣     [自然][美食][摄影][亲子] 多选
[生成行程需求单]
─────────────
提示：提交后顾问将在 24h 内为您回执方案
      （AI 自动规划将于后续版本上线）
```
- 提交 → 生成 `consult_record`（channel=智能需求单）→ 推后台。
- 二阶段：本页切换为「AI 自动生成方案 + 一键转报名」。
- **多次提交（V1.2 新增）**：提交成功后展示成功页，提供「再提交一份需求」按钮（保留已填字段，便于微调后补交另一份需求）；累计提交份数提示。后端 `consult_record` 按次新增，不做硬去重（不同内容可重复提交；如需防刷可对 `(user_id, content_hash)` 24h 去重或加提交冷却）。

### 4.6 我的 / 订单列表（Mine / Orders）
```
我的：
[头像] 微信昵称
我的订单 / 我的收藏 / 客服中心 / 关于顾问
订单列表项：线路名 + 状态标签 + 金额 + 日期
状态标签：待确认/已确认/待付定金/报名成功/完成
```
- 订单详情：行程信息 + 状态步骤条 + 定金确认提示（MVP 线下）+ 联系顾问。

### 4.7 客服 / 咨询页（Consult）
- MVP：展示「微信客服」按钮（跳转微信客服会话）+ 电话按钮 + 在线留言表单。
- 常见问题折叠列表（多少钱/出发时间/费用包含/人数）。

### 4.8 分享卡片（Share）
- 分享标题：「【顾问推荐】新疆15日深度游 ¥3999，已有28人报名」
- 分享图：线路封面 + 顾问头像 + 小程序码。
- 二阶段接入分销：分享带 `referrer_user_id`，成交后写 `commission`。

### 4.9 客户注册 / 登录页（V1.2 新增）
**目标**：沉淀客户身份，关联后续订单与收藏。

```
[顾问头像] 欢迎加入旅途管家
昵称 *       [____]
手机号 *     [____]
微信号       [____]
旅行偏好     [自然][美食][亲子] 多选
预算区间     [3000内][3000-6k][6k-1w][1w+]
[注册 / 登录]
```
- 按手机号去重：已存在则视为登录并返回 `user_id`/`customer_id`（幂等）。
- 注册/登录态写入全局，用于报名、收藏、智能规划的需求归属。
- 未注册用户浏览线路、提交需求仍可走游客态（需求单 `user_id` 可空）。

### 4.10 我的收藏页（V1.2 新增）
**目标**：集中查看已收藏线路，促进复访与报名。

```
[收藏线路卡片列表]
封面 + 名称 + 天数 + 价格 + [取消收藏]
空状态：插画 + 「还没有收藏，去发现心仪线路」
```
- 数据来自后端 `favorite`（user_id + route_id）；详情页「收藏」按钮实时 toggle。

### 4.11 关于顾问页（V1.2 新增）
**目标**：强化「真人顾问」信任感，提供联系入口。

```
顾问头像 + 姓名 + 服务年限/擅长线路
个人简介
[微信客服] [拨打电话] [复制微信号]
资质/服务承诺说明
```
- 入口：我的 → 关于顾问；首页顾问卡点击亦可进入。

---

## 5. 核心用户流程

### 5.1 浏览 → 报名
```
首页/Banner → 线路详情 → 报名表单 → 提交
→ 待确认 → 顾问确认 → 待付定金(线下)
→ 顾问确认收款 → 报名成功 → 完成
```

### 5.2 智能需求提交
```
智能规划页填需求 → 生成需求单 → 后台顾问查看
→ 人工回执方案（引用线路/定制） → 用户查看并报名
```

### 5.3 订单状态流转（MVP）
```
待确认 → 已确认 → 待付定金(线下) → 定金已收(顾问确认) → 报名成功 → 完成
```
（二阶段：`待支付定金 → 已支付 → 报名成功`）

---

## 6. Web 管理后台页面设计

### 6.1 登录
账号密码 + 图形验证码；角色区分（顾问/超管）；JWT 会话。

### 6.2 Dashboard
```
KPI 卡片：今日订单 / 本月收入 / 利润 / 客户增长（7天）
图表：热门线路 TOP5（条形图）、近30天订单趋势（折线/面积，纯前端渲染，无额外依赖）
待办：待确认订单、待收定金订单
```

### 6.3 线路管理
- 列表：名称/分类/价格/报名数/成本价/状态/操作（编辑/上下架）。
- 编辑表单：基础信息 + 成本价 + 利润（后台计算，不向前端暴露）+ 图集（多图 URL）+ 每日行程（route_day 动态增删）+ 费用说明 + 成团人数。
- 状态支持上架/下架一键切换。

### 6.4 订单管理
- 列表：订单号/线路/联系人/人数/状态/金额。
- 详情弹窗：用户信息 + 行程 + 状态步骤条 + 操作按钮（**确认订单 / 确认定金收款 / 完成**），按当前状态与角色显示（method=线下，记录 operator）。
- 状态流转操作受角色权限控制。

### 6.5 客户 CRM
- 列表：姓名/手机/来源/标签/历史订单/累计消费。
- 详情：资料编辑 + 标签管理 + 咨询/订单记录 + 自动提醒（二阶段）。

### 6.6 收益管理
- 利润统计（基于 route 的 price-cost）、订单明细（含每单利润）、顾问提成（二阶段）。

### 6.7 轮播管理（Banner，V1.2 新增）
- 列表：图片预览 / 标题 / 关联线路 / 排序 / 状态（上架/下架）。
- 操作：新增 / 编辑 / 删除 / 上下架切换。
- 小程序首页 Banner 读取已上架且按 sort 排序的记录。

---

## 7. 全局规范

### 7.1 状态页
- 空状态：插画 + 文案（如「还没有订单，去逛逛线路吧」）。
- 加载：骨架屏 / 顶部 loading。
- 错误：Toast 提示 + 重试。

### 7.2 弹窗与反馈
- 关键操作（提交报名/确认收款）用 Modal 二次确认。
- 成功用 Toast + 步骤推进。

### 7.3 权限与角色展示
- 用户端不区分角色（统一游客视角）。
- 管理后台按 `admin_user.role` 控制菜单与操作可见性。

### 7.4 微信生态适配
- 登录：wx.login 静默获取 openid。
- 订阅消息：订单状态变更、成团提醒模板。
- 分享：onShareAppMessage 配置卡片。

---

## 8. 与需求说明书对应表

| 需求章节 | 对应页面/模块 |
| --- | --- |
| 7.1 首页 | 4.1 首页 |
| 7.2 线路 | 4.2 / 4.3 |
| 7.3 智能规划 | 4.5 |
| 7.4 咨询 | 4.7 |
| 7.5 报名订单 | 4.4 / 4.6 / 5.1 |
| 7.6 CRM | 6.5 |
| 收藏 / 我的收藏 | 4.3 / 4.10 |
| 客户注册（扩展） | 4.9 |
| 关于顾问 | 4.11 |
| 7.8 会员 | 预留入口（二阶段 4.6 扩展） |
| 8 管理后台 | 6.1–6.7 |
| 8.3 轮播管理 | 6.7 |
| 9 数据库 | 各页字段来源（新增 banner/favorite 表） |
| 14 微信生态 | 7.4 |

---

## 9. 待确认/后续
- 是否需「会员中心」页（二阶段）。
- 真人对顾问头像/介绍素材提供方。
- 设计稿是否需输出高保真（Figma）与切图规范。
- 管理后台已采用 Vue3 + Element Plus 自建（非现成 Admin 模板）。
- **新增（V1.2）**：是否对智能规划需求单做 `(user_id, content_hash)` 24h 去重或提交冷却（防刷，不影响不同内容补交）。
- **新增（V1.2）**：客户标签前端编辑、自动提醒待二阶段完善。
