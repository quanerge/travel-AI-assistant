# 微信小程序需求说明书

## 旅途管家 —— 个人旅游顾问与线路推荐小程序

| 项 | 内容 |
| --- | --- |
| 版本 | V1.3（业务闭环 + 安全与权限增强，已与代码实现对齐） |
| 状态 | 已对齐实现（实现进度同步至 2026-07-29） |
| 最近更新 | 2026-07-29 |
| 作者 | 产品/技术联合起草 |

> 说明：本文件为对 `需求说明书_修订版V1.1.md` 的正式 V1.3 对齐版。原 V1.1 文档内容实际已演进到 V1.3 评审稿，但文件名与实现进度仍停留在 2026-07-27；本章将其规范化，并把 2026-07-29 当天落地的「全接口鉴权补丁、需求单业务闭环（MVP+P2+P3）、软删除、分页/限流/全局异常」等同步进实现进度跟踪。

### 版本变更记录

| 版本 | 日期 | 修订摘要 |
| --- | --- | --- |
| V1.0 | — | 初稿，存在会员矛盾、AI 与 MVP 冲突、合规缺失等问题 |
| V1.1 | 2026-07-23 | 统一会员等级与权益；明确 AI 规划在 MVP 的替代方案；新增合规、非功能需求、验收标准、微信生态、数据模型补全；重构章节层级 |
| V1.2 | 2026-07-24 | 实现补全记录：落地 JWT 鉴权、Banner 轮播、我的收藏、订单详情与「完成」状态流转、利润核算；新增客户注册、智能规划多次提交；新增 Banner/Favorite 数据模型；管理后台补齐看板图表、收益利润、线路成本价/图集/上下架、订单详情弹窗、Banner 管理页 |
| V1.3 | 2026-07-27 | 安全与权限增强（第一批）：手机号可逆加密存储（enc: 前缀）；小程序用户 JWT 鉴权；后台「用户管理」与「系统设置」页；生日/纪念日关怀提醒；微信身份持久化自动恢复登录态 |
| V1.3 | 2026-07-29 | 实现对齐补遗（第二批）：① 全接口鉴权补丁（只读/写接口统一 `get_current_admin`/`get_current_user`/`get_principal`，修复 PII 泄露与越权；`wx_login` 服务端换 openid 防伪造）；② 安全加固（CORS 收紧、JWT_SECRET/PHONE_ENCRYPT_KEY 环境变量化、登录限流、全局异常、列表分页）；③ 需求单业务闭环（MVP 站内方案+未读红点、P2 微信订阅消息、P3 附件/行程卡片/一键转订单）；④ 需求单与订单软删除（双角色鉴权+资金护栏）；⑤ 订单关联线路名展示、注册 409 幂等修复、小程序连真实后端、⑥ 客服消息回传管理后台 |

---

## 术语表

| 术语 | 说明 |
| --- | --- |
| 私域 | 旅游顾问通过微信（朋友圈/群/客服）沉淀的私有客户流量 |
| 成团 | 报名人数达到线路设定的最低成团人数后，线路确认发团 |
| 旅游顾问 | 平台运营者/达人，即「旅游顾问管理员」角色 |
| 分销/裂变 | 老用户分享带来新客成交后获得奖励的机制 |
| MVP | 最小可行产品，本说明书指 V1.0 首版上线范围 |
| 定金 | 用户报名后支付的部分款项，用于锁定名额 |
| 软删除 | 不物理删除数据，仅置 `is_deleted=True` + 记录 `deleted_at`，保留审计与支付痕迹，误删可恢复 |

---

# 1. 项目背景

## 1.1 当前业务模式

目前旅游业务主要依靠：

- 微信朋友圈发布线路
- 微信群推广
- 私聊咨询
- 人工报价
- 人工统计报名

业务流程：

```
朋友圈/微信群 → 客户咨询 → 人工推荐线路 → 报名成交
```

## 1.2 当前存在的问题

| 问题 | 影响 |
| --- | --- |
| 朋友圈内容容易沉底 | 客户无法长期查看 |
| 线路信息不统一 | 客户理解成本高 |
| 重复回答咨询 | 人工成本高 |
| 客户资料没有沉淀 | 无法二次营销 |
| 无法统计销售数据 | 无法优化线路 |
| 缺少品牌沉淀 | 收益增长有限 |

---

# 2. 产品定位与商业目标

## 2.1 产品定位

打造：**个人旅游顾问 + AI 路线规划（第二阶段） + 私域成交系统**。

不是传统旅游商城，而是帮助旅游达人利用微信私域流量实现持续收益增长的工具。

> 说明：AI 路线规划是本产品的长期核心差异化能力，但 V1.0 MVP 阶段暂以「智能需求提交 + 人工规划」方式实现（详见 7.3），正式 AI 自动规划于第二阶段上线。

## 2.2 商业目标

```
微信流量 → 旅游小程序 → 客户咨询 → 订单成交 → 客户复购 → 持续收益
```

---

# 3. 收益模式设计

## 3.1 线路销售利润

```
供应商价格：2800 元
销售价格：3300 元
利润：500 元/人
```
示例（20 人团）：500 × 20 = 10000 元利润。

## 3.2 私人定制旅游

| 类型 | 价格 | 说明 |
| --- | --- | --- |
| 普通规划 | 199 元 | 标准路线设计 |
| 高级定制 | 999 元 | 1v1 深度定制 |

服务：路线设计、酒店推荐、景点安排、交通规划、美食推荐。

## 3.3 会员体系（统一版，见第 7.8 节详细权益）

| 等级 | 价格 | 核心权益 |
| --- | --- | --- |
| 普通用户 | 免费 | 浏览线路、限额 AI 规划 |
| VIP 会员 | 99 元/年 | 不限 AI 规划、人工咨询免费、线路优惠 |
| 高级会员 | 299 元/年 | 在 VIP 基础上增加：专属 1v1 规划师、更高折扣、生日礼遇、可赠 1 张亲友 VIP |

> 注：会员权益矩阵以第 7.8 节为准，本节仅作总览。

## 3.4 分享裂变佣金（第二阶段上线）

```
A 用户 → B 用户购买线路 → A 获得奖励（现金/积分/优惠券）
```

---

# 4. 用户角色与权限

## 4.1 角色总览

| 角色 | 说明 | 账号归属 |
| --- | --- | --- |
| 普通游客 | 微信用户，浏览/搜索/咨询/报名 | `user` 表 |
| VIP / 高级会员 | 游客付费升级，享对应权益 | `user` + `member` 表 |
| 旅游顾问管理员 | 发布线路、管理客户、处理订单、查看收益 | `admin_user` 表（role=顾问） |
| 超级管理员 | 用户/权限/数据统计/系统配置 | `admin_user` 表（role=超管） |

## 4.2 权限矩阵

| 功能 | 游客 | VIP/高级 | 顾问管理员 | 超级管理员 |
| --- | --- | --- | --- | --- |
| 浏览/搜索线路 | √ | √ | √ | √ |
| AI 规划（MVP 为需求提交） | 限额 | 不限 | √ | √ |
| 人工咨询 | 收费 | 免费 | 处理 | √ |
| 提交报名 | √（凭用户 token） | √（凭用户 token） | — | — |
| 发布/编辑线路 | — | — | √ | √ |
| 管理客户(CRM) | — | — | √ | √ |
| 处理订单/确认定金 | — | — | √ | √ |
| 查看收益统计 | — | 本人 | √ | √ |
| 用户/权限/系统配置 | — | — | — | √ |
| 需求单回复/方案生成 | — | 查看自己的 | √ | √ |
| 软删除需求单/订单 | 仅自己的（受限） | 仅自己的（受限） | 任意 | 任意 |

> **V1.3 鉴权说明**：所有受保护接口均经依赖注入鉴权，用户 token 与管理员 token 通过 `payload.type` 字段（`user` / `admin`）隔离，防止越权调用；未携带合法 token 的受保护请求返回 401。

## 4.3 角色与数据表映射

- `user`：微信前台账号（openid 关联），游客与会员共用。
- `customer`：CRM 业务档案，通过 `user_id` 关联 `user`；纯线下客户可仅有 `customer`（`user_id` 为空）。
- `admin_user`：管理后台独立账号体系，与 `user` 隔离，不对外开放注册。

---

# 5. 合规与资质

> 本章为上线前置条件，须在开发启动前完成评估。

## 5.1 平台资质
- 微信小程序经营「旅游/旅行社」类目需提交《旅行社业务经营许可证》或在平台完成经营者资质审核。
- 若运营主体无旅行社资质，需以**已有资质的合作旅行社**名义上架，或通过「旅游攻略/信息咨询」类目规避交易资质风险（需法务确认）。
- 服务器域名须完成 ICP 备案，小程序 `request`/`upload` 域名须配置为微信合法域名。

## 5.2 业务合规
- 依据《在线旅游经营服务管理暂行规定》：明示费用构成、不得虚假宣传、对合作经营者资质尽审核义务。
- 线路详情须完整披露费用包含/不含项、退改规则。

## 5.3 个人信息保护
- 依据《个人信息保护法》：手机号、微信昵称、行程偏好等为个人信息，须：
  - 在隐私政策中告知收集目的与范围，获取用户授权；
  - 存储加密（手机号脱敏/加密落库）；
  - 提供查询、更正、删除入口。
- **手机号加密存储（V1.3 全量落地）**：`user` / `customer` / `order` / `consult_record` / `admin_user` 五张表的 `phone` 字段均以 `enc:` 前缀的可逆加密（标准库 XOR 流密码）落库，读取时解密，满足"存储加密"要求；密钥来自环境变量 `PHONE_ENCRYPT_KEY`，生产须独立配置，且兼容历史明文。
- **删除入口（V1.3 新增）**：需求单与订单均支持软删除（见 7.3 / 7.5 / 第 16 章），满足"提供删除入口"的合规要求，同时保留审计与支付痕迹。
- **接口安全（V1.3 新增）**：
  - 全接口鉴权：只读/写接口统一鉴权依赖，消除 PII 泄露与越权篡改（见 5.3 安全加固清单）。
  - CORS 收紧：默认仅允许 `http://localhost:5173` / `http://127.0.0.1:5173`；仅当显式配置 `*` 时才允许通配，且自动关闭 `credentials`，消除 `*`+credentials 隐患。
  - 登录限流：对 `/api/auth/login`、`/api/auth/wx-login` 按客户端 IP 做 60 秒/10 次滑动窗口限流，返回 429 + `Retry-After`。
  - 全局异常：注册 500（不泄露堆栈）、422、409（数据冲突）、显式 HTTPException 处理器。
  - 密钥环境变量化：`JWT_SECRET`、`PHONE_ENCRYPT_KEY` 经 `os.getenv` 读取，生产仅设环境变量即可，代码无硬编码密钥兜底暴露。

## 5.4 AI 输出免责
- AI（含 MVP 的人工规划回执）输出的费用预估、路线建议**仅供参考，不构成承诺**；涉及具体成交以线路详情与合同为准，页面须有「预估仅供参考」提示。

---

# 6. 系统架构

```
微信小程序（用户端）
        |
微信开放能力（登录/客服/订阅消息/分享/支付-二阶段）
        |
业务服务层（FastAPI）
        |
   ┌────┴────┐
 SQLite    （生产可切 MySQL，见 13.2）
        |
AI 推荐服务（第二阶段接入）
        |
管理后台（Web，Vue3 + Element Plus，顾问/超管使用，由后端托管 /ui）
```

> V1.3 备注：`order` 为 SQLite 保留字，迁移 SQL 已用双引号转义 `ALTER TABLE "order"`；新列经启动 `migrate()` 自愈加列，规避历史库缺列 500。

---

# 7. 用户端功能需求

## 7.1 首页模块

展示：品牌形象、热门线路、推荐活动、快捷入口。

页面结构：
```
旅游顾问头像 + 个人介绍
热门推荐（云南8日 / 新疆15日 / 川西自驾）
[智能行程规划]（MVP 为需求提交，见 7.3）
我的订单 / 联系客服
```

### Banner 轮播

| 字段 | 说明 |
| --- | --- |
| 图片 | 轮播图 |
| 标题 | 文案 |
| 跳转线路 | 关联的 route_id |
| 排序 | 展示顺序 |

### 推荐线路
展示：图片、名称、天数、价格、热度。

### 快捷入口
我要去哪里？ / 智能规划 / 热门路线 / 私人定制 / 我的订单。

## 7.2 旅游线路管理

### 线路分类
```
国内游（云南 / 新疆 / 西藏 / 四川）
短途游（周末游 / 自驾游）
境外游（日本 / 东南亚）
```

### 线路详情
```
线路名称 + 评分
图片/视频
出发地 / 天数 / 价格 / 已报名人数 / 成团人数
行程安排（Day1…DayN）
费用说明（含/不含）
注意事项
[立即报名]
```
> 价格展示以 `price` 为准；`cost_price`/`profit` 仅后台可见，不向前端暴露。

## 7.3 AI 旅游规划模块

### 用户输入
目的地、时间、人数、预算、兴趣（示例：不限 / 5天 / 夫妻2人 / 5000 / 自然·美食）。

### MVP（V1.0）实现方式 —— 智能需求提交
- 用户填写上述信息后，**不调用大模型**，系统生成结构化「行程需求单」并推送至旅游顾问；
- 顾问在后台查看需求单，人工回执路线建议（可引用已有线路或定制）；
- 首页入口文案为「智能行程规划（顾问 1v1 帮您规划）」，避免暗示全自动 AI。

### 需求单闭环（V1.3 业务闭环，详见第 16 章）
- **顾问回执方案**：后台对需求单回复 `reply_content`（方案正文），并可附 `attachments`（图片 URL 列表）与 `itinerary`（行程卡片 `[{day,title,desc}]`）；状态流转 `pending → replied`，确认闭环后 `done`。
- **客户获知（MVP 站内方案 + 红点）**：小程序「我的咨询」入口展示需求单与顾问方案，未读回复以红点提示；客户读取时标记已读（`customer_read_at`）。
- **微信订阅消息（P2）**：顾问回复成功后向客户推送微信订阅消息（模板字段 thing1 事项 / name2 处理人 / time3 时间），需在小程序 MP 后台配置订阅模板并将 template id 填入 `config.subscribeTemplateId`、后端配置 `WECHAT_APPID/WECHAT_SECRET/WX_SUBSCRIBE_TMPL_ID`；缺配或推送失败静默降级，仍走站内红点兜底。
- **一键转订单（P3）**：客户对方案满意可「对此方案下单」，后端从需求单生成 `pending_confirm` 订单（按 `route.price × person_count` 算总价、归集客户、需求单标记 `done` 闭环）。

### 第二阶段实现 —— AI 自动规划
- 接入大模型，结合线路库/知识库自动生成路线建议与费用预估；
- 输出含「推荐原因、费用拆分、预计总额、是否转人工顾问」；
- AI 输出须标注「预估仅供参考」（见 5.4）。

## 7.4 咨询模块

支持：微信客服、电话咨询、在线留言。

自动回复（常见问题）：多少钱？什么时候出发？包含哪些费用？几个人？

> MVP 阶段以微信客服 + 在线留言为主；电话为展示号码，由顾问人工接听。

## 7.5 报名订单模块

### 用户提交信息
姓名、手机号、人数、出发日期、备注。

### 订单状态（MVP）
```
待确认(pending_confirm) → 已确认(confirmed) → 待付定金(pending_deposit) → 定金已收(deposit_paid) → 报名成功(success) → 完成(completed)
```
- **MVP 不接在线支付**：定金通过微信转账/线下方式支付，顾问在后台「确认收款」后订单推进。
- 第二阶段接入微信支付后，状态改为：`待支付定金 → 已支付 → 报名成功`。
- **订单线路名展示（V1.3）**：订单列表与详情回填关联线路名称 `route_name`，便于顾问与客户识别。

### 订单软删除（V1.3，合规 + 防垃圾数据）
- 客户仅可删除**自己**的订单，且须满足护栏：状态为 `pending_confirm` / `pending_deposit` 且 `deposit_paid != True`（已确认/已收定金/已完成拦截，提示"联系顾问"），避免资金与履约数据悬空。
- 管理员可删除任意订单。
- 软删除仅置 `is_deleted=True`，列表/详情默认过滤已删。

## 7.6 客户 CRM 管理

### 客户信息（customer 表）
姓名、微信昵称、手机号、来源、旅游偏好、预算区间、生日/纪念日（`birthday` MM-DD，用于关怀提醒）、跟进状态（`follow_status`）、最后联系时间、顾问备注、历史订单（关联 order）、客户标签、累计订单数与金额、上次出行。

### 客户标签
家庭旅游 / 情侣旅游 / 高消费 / 自驾用户 / 自然风景爱好者。

### 自动提醒
- 生日/纪念日关怀：基于 `customer.birthday` 在 Dashboard 与后台提醒；小程序「我的」可编辑生日。
- 示例（二阶段）：王先生去年云南游 → 今年推荐新疆线路（基于 `last_trip` 触发）。

## 7.7 营销裂变（第二阶段）

### 分享推广
用户分享线路 → 好友购买 → 获得积分/优惠券/现金奖励（写入 `commission` 表）。

### 拼团功能
示例：新疆15日，已有 8 人，还差 4 人成团（基于 `group_size` 与 `signup_count` 计算）。

## 7.8 会员系统（统一版）

### 会员等级

| 等级 | 价格 | 权益 |
| --- | --- | --- |
| 普通用户 | 免费 | 浏览线路、AI 规划限额（MVP 为需求提交限额） |
| VIP 会员 | 99 元/年 | 不限 AI 规划、人工咨询免费、线路优惠、优先报名 |
| 高级会员 | 299 元/年 | 在 VIP 基础上：专属 1v1 规划师、更高折扣、生日礼遇、可赠 1 张亲友 VIP |

### 权益对照

| 功能 | 普通 | VIP | 高级 |
| --- | --- | --- | --- |
| 浏览线路 | √ | √ | √ |
| AI 规划 | 限额 | 不限 | 不限 |
| 人工咨询 | 收费 | 免费 | 免费+优先 |
| 线路优惠 | — | √ | 更高 |
| 专属规划师 | — | — | √ |

> MVP 阶段：会员购买与权益自动化**暂不开发**（见第 12 章），仅在前端预留入口与说明，避免承诺未上线功能。

---

# 8. 管理后台

## 8.1 载体与技术栈
- 独立 **Web 管理后台**（非小程序），技术栈：Vue3 + Element Plus + Vite，由后端 `main.py` 托管已构建的 `admin/dist/`（挂载 `/ui`）。
- 鉴权：管理员账号密码登录 + JWT；`admin_user` 表隔离，不开放注册。

## 8.2 Dashboard
今日订单、本月收入、热门线路、客户增长、利润统计、生日/纪念日关怀提醒、待办。

## 8.3 管理菜单
```
后台管理
├ 用户管理（超管专属：账号 CRUD、启用/停用、重置密码、改角色）
├ 客户 CRM（列表/详情/标签/跟进/生日提醒）
├ 线路管理（发布/编辑/改价/成本价/图集/上下架）
├ 订单管理（确认定金/状态流转/完成/删除/线路名展示）
├ 收益管理（利润核算）
├ 轮播管理（Banner 前后台 CRUD）
├ 智能需求单（查看/回复方案/附附件行程/一键转订单/删除）
├ 营销管理（二阶段）
├ AI 配置（第二阶段）
└ 系统设置（环境/安全状态、合规清单、备份提示）
```

---

# 9. 数据库设计

## 9.1 表清单与字段

> V1.3 更新：补全 `customer` 的 `birthday/follow_status/last_contact_at/remark`，`order` 与 `consult_record` 的软删除与方案字段，`consult_record` 的 `route_id/name/phone`；所有 `phone` 字段均为加密存储。

### user（微信用户）
| 字段 | 说明 |
| --- | --- |
| id | 主键 |
| openid | 微信 openid（unique） |
| unionid | 微信 unionid（可选） |
| nickname | 昵称 |
| avatar | 头像 |
| phone | 手机号（**加密 enc:**） |
| status | 状态（active / disabled，后台可启用停用） |
| created_at | 注册时间 |

### customer（CRM 客户档案）
| 字段 | 说明 |
| --- | --- |
| id | 主键 |
| user_id | 关联 user（可空） |
| name | 姓名 |
| wechat_no | 微信昵称/号 |
| phone | 手机号（**加密 enc:**） |
| source | 来源 |
| travel_preference | 旅游偏好 |
| budget_range | 预算区间 |
| birthday | 生日/纪念日（MM-DD，用于关怀提醒） |
| tags | 客户标签 |
| remark | 顾问备注 |
| follow_status | 跟进状态（pending_follow / contacting / deal / lost） |
| last_contact_at | 最后联系时间 |
| total_orders | 历史订单数 |
| total_amount | 累计消费 |
| last_trip | 上次出行 |
| created_at / updated_at | 时间 |

### route（线路）
| 字段 | 说明 |
| --- | --- |
| id | 主键 |
| name | 线路名称 |
| category | 分类 |
| cover | 封面 |
| gallery | 图集/视频（JSON） |
| days | 天数 |
| departure | 出发地 |
| destination | 目的地 |
| price | 销售价格（前端展示） |
| cost_price | 成本价格（后台仅） |
| profit | 利润（后台计算/缓存，不前端暴露） |
| rating | 评分 |
| signup_count | 已报名人数 |
| group_size | 成团人数 |
| status | 状态（active / inactive） |
| description / fee_included / fee_excluded / notice | 详情文案 |
| created_by / created_at | 创建信息 |

### route_day（每日行程）
| 字段 | 说明 |
| --- | --- |
| id | 主键 |
| route_id | 关联 route |
| day_no | 第几天 |
| title | 标题 |
| content | 内容 |
| meals | 餐饮 |
| accommodation | 住宿 |
| traffic | 交通 |

### order（订单）
| 字段 | 说明 |
| --- | --- |
| id | 主键 |
| order_no | 订单号（unique） |
| user_id | 关联 user |
| route_id | 关联 route |
| name / phone | 联系人（phone **加密 enc:**） |
| person_count | 人数 |
| departure_date | 出发日期 |
| remark | 备注 |
| status | 订单状态（见 7.5） |
| deposit_amount | 定金金额 |
| deposit_paid | 定金是否已收 |
| total_amount | 总金额 |
| is_deleted | 软删除标记（Boolean，默认 False） |
| deleted_at | 删除时间（可空） |
| created_at / updated_at | 时间 |

### payment（支付记录）
| 字段 | 说明 |
| --- | --- |
| id | 主键 |
| order_id | 关联 order |
| type | 定金/尾款（deposit / balance） |
| amount | 金额 |
| method | 微信/线下（wechat / offline） |
| trade_no | 交易号 |
| status | 状态 |
| paid_at | 支付时间 |
| operator_id | 线下确认人（admin_user） |

### member（会员）
| 字段 | 说明 |
| --- | --- |
| id | 主键 |
| user_id | 关联 user |
| level | 普通/VIP/高级（normal / vip / premium） |
| expire_at | 到期时间 |
| status | 状态 |
| created_at | 开通时间 |

### commission（佣金/分销）
| 字段 | 说明 |
| --- | --- |
| id | 主键 |
| order_id | 关联 order |
| referrer_user_id | 推荐人 |
| type | 现金/积分/优惠券（cash / points / coupon） |
| amount | 金额 |
| status | 状态 |
| settled_at | 结算时间 |

### coupon（优惠券）
| 字段 | 说明 |
| --- | --- |
| id | 主键 |
| code | 券码（unique） |
| user_id | 关联 user |
| amount | 面额 |
| condition | 使用条件 |
| expire_at | 过期时间 |
| status | 状态（unused / used） |

### consult_record（智能需求单 / 咨询记录）
| 字段 | 说明 |
| --- | --- |
| id | 主键 |
| user_id | 关联 user（可空） |
| channel | 渠道（wechat / phone / message / plan） |
| name / phone | 留言人姓名/手机（phone **加密 enc:**，用于归集客户） |
| content | 需求内容 |
| route_id | 关联线路（可空，用于智能需求单关联线路） |
| handled_by | 处理人（admin_user） |
| status | 状态（pending / replied / done） |
| reply_content | 顾问方案/回复正文 |
| reply_at | 顾问回复时间 |
| reply_by | 回复顾问 admin id |
| customer_read_at | 客户已读时间（用于未读红点） |
| attachments | 顾问方案附件（JSON 图片 URL 列表） |
| itinerary | 行程卡片（JSON `[{day,title,desc}]`） |
| is_deleted | 软删除标记（Boolean，默认 False） |
| deleted_at | 删除时间（可空） |
| created_at | 时间 |

### admin_user（管理员）
| 字段 | 说明 |
| --- | --- |
| id | 主键 |
| username | 账号（unique） |
| password_hash | 密码哈希 |
| role | 顾问/超管（advisor / super） |
| phone | 手机号（**加密 enc:**） |
| status | 启用/停用（active / disabled） |
| created_at | 创建时间 |

### banner（首页轮播）
| 字段 | 说明 |
| --- | --- |
| id | 主键 |
| image | 图片 URL |
| title | 文案 |
| route_id | 关联跳转线路（可空） |
| sort | 展示排序 |
| status | 上架/下架（active / inactive） |

### favorite（我的收藏）
| 字段 | 说明 |
| --- | --- |
| id | 主键 |
| user_id | 关联 user |
| route_id | 关联 route |
| created_at | 收藏时间 |

### follow_up（客户跟进记录）
| 字段 | 说明 |
| --- | --- |
| id | 主键 |
| customer_id | 关联 customer |
| admin_id | 跟进人（管理员，可空） |
| content | 跟进内容 |
| created_at | 时间 |

---

# 10. 非功能性需求

| 类别 | 要求 |
| --- | --- |
| 性能 | 预估日活 ≤ 1 万，峰值并发 ≤ 500；核心接口响应 < 500ms |
| 安全 | 全站 HTTPS；手机号加密存储（全表）；管理员密码加盐哈希；**全接口鉴权与防越权（V1.3 已落地）**；CORS 收紧；登录限流防刷；全局异常处理 |
| 可用性 | 目标 99.5%；依赖服务（DB）有容错与告警 |
| 日志监控 | 关键操作留痕（订单/支付/登录）；异常告警 |
| 数据备份 | SQLite 每日备份（生产建议切 MySQL 并每日备份，保留 ≥ 7 天） |
| 合规 | 满足第 5 章资质与个保要求；提供删除入口（软删除） |

---

# 11. 验收标准（要点）

| 模块 | 验收要点 |
| --- | --- |
| 首页 | Banner/推荐线路/快捷入口可配置并正确跳转 |
| 线路详情 | 价格、成团人数、费用说明完整展示；成本字段不暴露 |
| 智能需求提交 | 用户提交后生成需求单并推送到后台；顾问可回执方案（含附件/行程） |
| 需求单闭环 | 顾问回复后小程序「我的咨询」出现红点 → 客户查看方案 → 可一键转订单 |
| 报名订单 | 线下定金流程：顾问确认收款后状态正确流转；列表/详情显示线路名 |
| CRM | 报名/咨询自动沉淀客户；标签/生日/跟进可编辑 |
| 管理后台 | 发布线路、改价、确认定金、查看收益可用且权限隔离 |
| 安全 | 未授权请求被拦截（401）；手机号落库为 enc: 密文；登录接口限流生效 |
| 软删除 | 客户可删自己的未付定金订单；已确认/已收定金订单被拦截；需求单已转订单不可删 |
| 会员(二阶段) | 等级-权益矩阵与支付、到期逻辑正确 |

---

# 12. 分阶段规划

## 12.1 V1.0 MVP（必须实现）
用户端：首页、线路展示、线路详情、在线咨询、报名登记（线下定金）、分享线路、智能需求提交与闭环（V1.3 已增强）。
管理端：发布线路、修改价格、查看客户、查看订单（含确认定金、删除、线路名）、智能需求单回复与转订单、用户管理、系统设置。
**暂不开发**：在线支付、分销系统、AI 自动规划、会员体系（仅预留入口）。

## 12.2 第二阶段
AI 旅游规划（自动）、会员体系、拼团、优惠券、自动营销、视频号引流、微信支付。

## 12.3 第三阶段
个人旅游品牌 + 旅游内容平台 + 私域成交系统。

---

# 13. 技术方案

## 13.1 前端
- 用户端：微信小程序（WXML / WXSS / JavaScript）；`utils/config.js` 的 `useMock` 须为 `false` 以连接真实后端，`baseUrl` 指向后端（模拟器同机用 `http://127.0.0.1:8000/api`；真机预览改局域网 IP 并勾选"不校验合法域名"；正式发布须 https + 备案域名并配置 request 合法域名）。
- 管理端：Web（Vue3 + Element Plus + Vite），构建产物 `admin/dist/` 由后端托管。

## 13.2 后端
FastAPI + SQLAlchemy + SQLite（开发/演示；生产可经 `DATABASE_URL` 切 MySQL）+ 微信开放能力（二阶段 AI 服务）。
> 注：部署须 `npm run build` 重建 `admin/dist` 后由后端托管；管理后台纯静态预览无 `/api` 无法登录，须配合后端运行。

## 13.3 部署
- 开发：双击 `deploy.bat`（杀占用 8000 端口旧进程 + 前端构建 + 重启后端触发迁移）；轻量重启用 `restart-backend.bat`。
- 生产：Docker（server/Dockerfile + docker-compose）或 gunicorn + nginx 反代 `/api`；域名需 ICP 备案并配置微信合法域名。

---

# 14. 微信生态能力

| 能力 | 阶段 | 状态（V1.3） | 说明 |
| --- | --- | --- | --- |
| 微信登录 | V1.0 | ✅ 已实现 | wx.login → code → 服务端 `jscode2session` 换 openid（生产）；演示兜底仅信任服务端已落库 openid，防伪造 |
| 微信客服消息 | V1.0 | ✅ 已实现 | 咨询接入；客户消息经回调回传管理后台（会话列表+回复），明文模式（受 cryptography 约束） |
| 订阅消息 | V1.0 | ✅ 已实现（P2 可配置） | 顾问回复需求单后推送（thing1/name2/time3）；需 MP 后台模板 + env 配置；失败静默降级 |
| 分享卡片 | V1.0 | ✅ 已实现 | 线路分享带图与标题 |
| 微信支付 | 二阶段 | ⬜ 未实现 | 定金/尾款在线支付 |
| 视频号引流 | 二阶段 | ⬜ 未实现 | 内容导流 |

---

# 15. 风险与里程碑

## 15.1 主要风险
- **资质风险（高）**：无旅行社资质可能无法过审，须提前确认类目方案。
- **范围风险（中）**：AI 为核心卖点但延后至二阶段，首版差异化弱，需以「顾问 1v1」补偿体验。
- **合规风险（中）**：个保与广告合规须同步落地（V1.3 已补加密、鉴权、删除入口、CORS/限流/全局异常）。

## 15.2 里程碑（建议）
- M1：需求冻结 + 资质方案确认
- M2：MVP 用户端 + 管理端可用
- M3：MVP 上线（微信审核通过）
- M4：第二阶段功能迭代

---

# 16. 实现进度跟踪（V1.3，已与代码对齐）

> 本章记录相对 V1.1 需求说明书的已落地功能，便于需求与实现对齐。状态：✅ 已实现 / 🔶 部分实现 / ⬜ 未实现（属二阶段或暂不开发）。最近同步：2026-07-29。

## 16.1 用户端小程序

| 功能 | 对应需求 | 状态 | 说明 |
| --- | --- | --- | --- |
| 首页 Banner 轮播 | 7.1 | ✅ | 后端 Banner API + 小程序首页轮播，点击跳线路 |
| 线路列表与筛选（天数/出发地/价格） | 7.2 | ✅ | 筛选能力已实现 |
| 线路详情（收藏/分享/图集） | 7.2 | ✅ | 收藏 toggle、分享卡片、图集展示 |
| 智能规划需求提交 | 7.3 | ✅ | 生成 consult_record 推送后台 |
| 智能规划多次提交 | 7.3 | ✅ | 成功后「再提交一份需求」保留字段；后端按次新增 |
| 报名表单（线下定金） | 7.5 | ✅ | 提交进入待确认 |
| 客户注册/登录 | （扩展） | ✅ | register 页按手机号+openid 双重幂等去重（修复 409） |
| 我的：订单/收藏/关于顾问 | 4.1 / 7.6 | ✅ | 我的收藏、关于顾问页；订单列表展示线路名 |
| 微信静默登录 | 14 | ✅ | wx.login → 服务端 jscode2session（生产）换 openid；本地持久化自动恢复 |
| 我的咨询（需求单闭环） | 7.3 | ✅ | 列表+展开看方案/行程/附件+未读红点；「对此方案下单」一键转订单；「删除此咨询」（已转订单不可删） |
| 我的订单删除 | 7.5 / 5.3 | ✅ | 仅自己的未付定金订单可删（护栏拦截已确认/已收定金） |

## 16.2 管理后台（Web）

| 功能 | 对应需求 | 状态 | 说明 |
| --- | --- | --- | --- |
| JWT 账号登录 | 8.1 | ✅ | bcrypt + JWT，拦截器携带 Bearer |
| Dashboard 看板 | 8.2 | ✅ | 今日订单/本月收入/利润/客户增长/TOP5/近30天趋势/待办/生日提醒 |
| 线路管理（成本价/图集/上下架） | 8.3 | ✅ | 成本价/利润/图集、上架下架切换 |
| 订单管理（详情/确认/完成/删除/线路名） | 8.3 | ✅ | 详情弹窗 + 确认订单/确认定金/完成/删除；列表与详情显示线路名 |
| 客户 CRM | 7.6 | ✅ | 列表/详情/标签/跟进/生日编辑（V1.3 补全 birthday/follow_status 等） |
| 收益管理（利润） | 8.2 | ✅ | 基于 route.price-cost 计算利润 |
| 轮播管理（Banner） | 8.3 | ✅ | 后台 Banner CRUD |
| 智能需求单（回复/附件/行程/转订单/删除） | 7.3 | ✅ | 回复对话框（方案正文+行程卡片编辑器+附件上传）；列表附件/行程计数；一键转订单；删除（二次确认） |
| 用户管理（账号 CRUD） | 8.3 | ✅ | 超管专属：账号新增/删除、重置密码、改角色、启用/停用（禁止操作自身） |
| 系统设置 | 8.3 | ✅ | 环境/安全配置状态展示、上线合规清单、备份提示 |
| 生日/纪念日关怀提醒 | 7.6 | ✅ | Dashboard 与后台生日提醒；小程序「我的」可编辑生日 |

## 16.3 后端服务

| 能力 | 状态 | 说明 |
| --- | --- | --- |
| JWT 鉴权 | ✅ | Authorization: Bearer；管理员 token 12h、用户 token 30 天；payload.type 隔离 user/admin |
| 微信登录（服务端换 openid） | ✅ | /api/auth/wx-login 生产走 jscode2session，防客户端伪造 openid |
| Banner / 收藏 API | ✅ | GET 首页 Banner、收藏 toggle/列表 |
| 订单详情 / 确认 / 完成 / 删除 API | ✅ | GET /orders/{id}、confirm、complete、delete（双角色+护栏） |
| 客户注册 API | ✅ | POST /api/customers/register，手机号+openid 双重幂等 |
| 利润核算 | ✅ | Dashboard 利润 = Σ(price-cost)×person |
| 手机号加密存储（全表） | ✅ | user/customer/order/consult_record/admin_user 手机号 enc: 前缀可逆加密，读取解密，兼容历史明文 |
| 用户管理 API | ✅ | GET/POST/PUT/DELETE /api/admin/users，超管专属 |
| 系统设置 API | ✅ | GET /api/admin/settings 返回环境/安全状态（不回显密钥明文） |
| 需求单闭环 API | ✅ | GET /mine、GET /unread-count、POST /{id}/read、PUT /{id} 回复、POST /{id}/to-order、POST /{id}/delete |
| 微信订阅消息 | ✅ | utils/wechat.py 发送订阅消息，env 可配，失败静默降级 |
| 附件上传 | ✅ | POST /api/upload/file（管理员），存 static/attachments |
| 列表分页 | ✅ | page/page_size，分页元信息写响应头；page 缺省返回全量（兼容小程序） |
| 登录限流 | ✅ | /api/auth/login、/api/auth/wx-login 按 IP 60s/10 次滑动窗口 |
| 全局异常 | ✅ | 500/422/409/HTTPException 统一处理 |
| CORS 收紧 | ✅ | 默认白名单 localhost:5173；`*` 模式下关闭 credentials |
| 密钥环境变量化 | ✅ | JWT_SECRET、PHONE_ENCRYPT_KEY 经 os.getenv 读取 |
| 自愈迁移 | ✅ | 启动 migrate() 加列；提交 try/except 捕获缺列→migrate 补列重试；order 保留字双引号转义 |

## 16.4 未实现（按计划属二阶段 / 暂不开发）

| 功能 | 计划 |
| --- | --- |
| 在线支付（微信支付） | 二阶段 |
| 分销 / 裂变佣金 | 二阶段 |
| AI 自动规划 | 二阶段 |
| 会员体系购买与权益 | 二阶段（仅预留入口） |
| 客户标签前端编辑（自动提醒） | 🔶 部分，二阶段完善 |
| 视频号引流 | 二阶段 |

> 注：订阅消息（原 V1.1 列为未接入）已在 V1.3 P2 实现，移至「已实现」。

## 16.5 2026-07-27 安全与权限增强（第一批）

| 功能 | 对应需求 | 状态 | 说明 |
| --- | --- | --- | --- |
| 手机号加密存储 | 5.3 | ✅ | user/customer/order 手机号 enc: 前缀可逆加密落库（V1.3 后续补全 consult_record/admin_user） |
| 小程序用户 JWT 鉴权 | 8.1 / 14 | ✅ | wx-login 签发用户 token（30 天）；收藏等接口凭 token 鉴权，与管理员 token 通过 payload.type 隔离防越权 |
| 后台用户管理（账号 CRUD） | 8.3 | ✅ | 超管专属：账号新增/删除、重置密码、改角色、启用/停用（禁止操作自身防锁死） |
| 后台系统设置页 | 8.3 | ✅ | 环境/安全配置状态展示、上线合规清单、备份提示 |
| 生日/纪念日关怀提醒 | 7.6 | ✅ | 客户 birthday(MM-DD) 驱动；Dashboard 与后台生日提醒；小程序「我的」可编辑生日 |
| 微信身份持久化自动恢复 | 14 | ✅ | openid 存本地，重启/退出后静默登录自动恢复登录态 |

## 16.6 2026-07-29 全接口鉴权与安全加固（第二批·P0/P1）

| 功能 | 对应需求 | 状态 | 说明 |
| --- | --- | --- | --- |
| 全接口鉴权补丁 | 5.3 / 10 | ✅ | 只读接口（customers/orders/consult 列表、upload 等）补 `get_current_admin`；写接口（routes/orders/customers/upload）统一鉴权；用户接口统一 `get_current_user`/`get_principal`；修复 openid 可伪造漏洞 |
| CORS 收紧 | 5.3 / 10 | ✅ | 默认白名单 localhost:5173；`*` 模式自动关闭 credentials |
| 密钥环境变量化 | 5.3 | ✅ | JWT_SECRET / PHONE_ENCRYPT_KEY 经 os.getenv 读取，生产仅设环境变量 |
| 登录限流 | 10 | ✅ | /api/auth/login、/api/auth/wx-login 按 IP 60s/10 次滑动窗口，429 + Retry-After |
| 全局异常处理 | 10 | ✅ | 500（不泄露堆栈）/ 422 / 409 / 显式 HTTPException 处理器 |
| 列表分页 | 10 | ✅ | page/page_size（默认 50/上限 200），分页元信息写响应头 X-Total-Count/X-Page/X-Page-Size；page 缺省返回全量（向后兼容小程序）。涉及 customers/orders/consult/routes/banners/users |
| 注册 409 修复 | 7.6 | ✅ | register_customer 对手机号与 openid 双重幂等，避免 openid unique 冲突 |
| 小程序连真实后端 | 13.1 | ✅ | config.js useMock:false，baseUrl 接后端；真机需局域网 IP + 不校验域名 |

## 16.7 2026-07-29 需求单业务闭环（MVP + P2 + P3）

| 功能 | 对应需求 | 状态 | 说明 |
| --- | --- | --- | --- |
| MVP 站内方案 + 红点 | 7.3 | ✅ | 顾问回执 reply_content，流转 pending→replied；客户 GET /mine 查看、GET /unread-count 未读计数、POST /{id}/read 标记已读；小程序「我的咨询」红点 |
| P2 微信订阅消息 | 7.3 / 14 | ✅ | utils/wechat.py 发送（thing1/name2/time3）；env 配 WECHAT_APPID/SECRET/WX_SUBSCRIBE_TMPL_ID；失败静默降级；小程序提交时 wx.requestSubscribeMessage 授权 |
| P3 附件 / 行程卡片 | 7.3 | ✅ | consult_record 加 attachments(JSON 图片 URL 列表)、itinerary(JSON [{day,title,desc}])；POST /api/upload/file 管理员上传 |
| P3 一键转订单 | 7.3 | ✅ | POST /{consult_id}/to-order 生成 pending_confirm 订单（route.price×person_count，归集客户，需求单标记 done） |
| 咨询记录姓名/手机显示 | 7.3 | ✅ | _attach_customer_identity 回退（需求单缺姓名手机时取关联 Customer/User） |

## 16.8 2026-07-29 软删除（合规 + 防垃圾数据）

| 功能 | 对应需求 | 状态 | 说明 |
| --- | --- | --- | --- |
| 需求单软删除 | 5.3 / 7.3 | ✅ | consult_record 加 is_deleted/deleted_at；POST /{id}/delete 双角色（管理员删任意；客户仅删自己且 status!='done'）；列表/我的咨询/未读计数/转订单均过滤已删 |
| 订单软删除 | 5.3 / 7.5 | ✅ | order 加 is_deleted/deleted_at；POST /{order_id}/delete 双角色（管理员删任意；客户仅删自己且 deposit_paid!=True 且 status∈{pending_confirm,pending_deposit}，已确认/已收定金拦截）；列表/详情过滤已删 |
| 自愈迁移（order 保留字） | 6 / 9 | ✅ | ALTER TABLE "{table}" 双引号转义；新列经启动 migrate() 加列；提交 try/except 捕获缺列→migrate 补列重试，规避历史库 500 |

## 16.9 已知小瑕疵（非崩溃，可选优化）
- 同一 openid 换不同手机号注册会生成两个 Customer 归属同一 User；暂不影响 409，后续可加「按 openid 复用其已有 Customer」优化。
- 订单删除后若已产生 Payment/Commission，当前未做级联标记；客户侧删除后 orderDetail 页打开应优雅提示「已删除」（当前 get_order 已对删除单返回 404）。
- attachments/itinerary 在部分实现中以 TEXT 承载 JSON（ORM JSON 类型自动序列化/反序列化），与文档 JSON 描述一致。

## 16.10 2026-07-29 客服消息回传管理后台（第三批）

| 功能 | 对应需求 | 状态 | 说明 |
| --- | --- | --- | --- |
| 微信客服回调接入 | 14 | ✅ | 新增 `routers/chat.py`：`GET/POST /wechat/callback` 接收客户在「微信客服」会话发的消息。明文模式（受 cryptography 约束不能做 AES 解密，公众平台须设「消息加解密=明文」）；GET 校验 signature 回显 echostr，POST 解析 XML 存 `chat_message(direction=in)`，失败仍回 `success` 防重试 |
| 客服消息数据模型 | 9.1 | ✅ | `models.py` 新增 `ChatMessage`（openid / direction in·out / msg_type text / content / admin_id / is_read / created_at）；启动时 `create_all` 自动建表 |
| 后台会话列表 | 8.3 | ✅ | `GET /admin/chat/sessions`（管理员鉴权）按 openid 聚合，返回最近消息、未读数、关联客户昵称/手机（openid 匹配 user 表） |
| 后台消息记录 | 8.3 | ✅ | `GET /admin/chat/messages?openid=` 返回某会话全部消息（时间正序） |
| 管理员回复 | 8.3 / 14 | ✅ | `POST /admin/chat/reply` 存出站消息 + 调 `wechat.send_custom_message` 下发（48h 互动限制内可达，失败静默降级）；`POST /admin/chat/read` 标记已读清红点 |
| 后台客服消息页 | 8.3 | ✅ | 新建 `admin/src/views/Chat.vue`：左侧会话列表（昵称/手机/未读红点/搜索）、右侧气泡（in 左 out 右）、底部回复框（回车发送）、12s 轮询刷新；`router` + `MainLayout` 菜单接入 |
| 版本号（后端化） | 8 | ✅ | `GET /version` + 前端拉取（上一轮已落地），版本号单一来源在后端 env `APP_VERSION` |

> 约束说明：受本项目「requirements 不含 cryptography」约束，客服消息回调仅支持**明文模式**，无法支持微信安全/兼容模式的 AES 解密；且微信用户消息回传依赖**公网可达的回调 URL**（本地 FastAPI 无公网地址，需 Docker 生产部署或内网穿透才能真跑通）。

---

## 16.11 2026-07-29 静态审查发现缺陷修复（第四批）

| 编号 | 类型 | 位置 | 问题 | 修复 |
| --- | --- | --- | --- | --- |
| B1 | 功能缺陷 | `miniprogram/pages/plan/plan.js` `plan.wxml` `plan.wxss` | 「智能规划」表单未收集联系人姓名/手机，`consult_to_order` 校验姓名手机非空必返回 400，导致从智能规划提交的需求单无法一键转订单 | 表单新增「联系人/手机号」输入（手机 `type=number`）；submit 校验姓名手机非空 + 手机号正则 `^1\d{10}$`；content 拼入联系人/手机；`submitPlan` 透传 name/phone 至后端 `ConsultCreate`（create_consult 已持久化到 `ConsultRecord.name/phone`） |
| B2 | 设计缺陷 | `server/routers/chat.py` | 微信回调 GET 校验「无论 signature 是否匹配都回显 echostr」，Token 配错也能「配置成功」，掩盖配置错误 | 改为仅 `sha1(token+timestamp+nonce)==signature` 才回显 echostr，否则返回 HTTP 403（顶部 import 补 `status`；校验异常 sha 置空走 403 分支） |
| B3 | 正确性缺陷 | `server/routers/consult.py`（`create_consult`+`update_consult`）、`server/routers/orders.py`（`delete_order`） | 兜底补列逻辑 `except` 内未先 `db.rollback()` 即二次 `commit()`，存在悬挂事务风险 | 兜底改为先 `db.rollback()` 再 `migrate()` 重试；`create_consult` 在 rollback 后 `re-add(rec)` 保证 pending 对象不丢失 |
| B4 | 正确性缺陷 | `server/routers/orders.py` `confirm_deposit` | `o.updated_at = None` 会让模型 `onupdate` 列被显式置 NULL，更新时间丢失 | 删除该行，改由 `Order.updated_at` 的 `onupdate=datetime.utcnow` 在提交时自动维护 |

> 修复均经静态代码审查确认（本环境命令通道故障，未做运行验证）；双击 `deploy.bat` 重建 `admin/dist` + 重启后端后生效。验证重点（B1）：走「智能规划」填姓名手机提交 → 后台回复 → 小程序「我的咨询」一键转订单应成功。

---

# 总结

本项目目标不是建设传统旅游商城，而是打造：

> 一个属于个人旅游顾问的数字化销售增长工具。

通过微信私域流量、小程序沉淀、AI 推荐（二阶段）和 CRM 运营，实现：提高成交率、降低人工成本、增加客户复购、扩大个人旅游品牌影响力。

**V1.3 对齐要点**：在 V1.1 统一会员权益、厘清 AI 规划 MVP 替代路径、补齐合规与非功能需求、补全数据库字段与角色映射的基础上，V1.3 进一步落地了（1）全接口鉴权与多项安全加固（CORS/限流/全局异常/分页/密钥环境变量化）；（2）需求单业务闭环（站内方案+红点、微信订阅消息、附件/行程、一键转订单）；（3）需求单与订单软删除（双角色鉴权+资金护栏）；（4）订单线路名展示、注册幂等、小程序连真实后端。文档数据库设计与实现进度已同步至 2026-07-29 的实际代码状态。
